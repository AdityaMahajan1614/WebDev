<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/index.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@1,800&family=Roboto&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@1,800&display=swap" rel="stylesheet">
    <title>Blogger</title>
</head>
<body>
    <div class="navbar">
        <div class="name">
            Blogger
        </div>    
        <div class="navigation">
           <a href="/" class="home">Home</a> <a href="/create">Blog</a> 
        </div>
    </div>
    <% if(locals.nameList.length!==0){ %>
        <% for(var i=nameList.length-1;i>=0;i--){ %>
            <% if(nameList[i]==='') {
                continue;
            } else{%>
            <div class="blogbox">
                <h3><%= nameList[i] %></h3> 
                <p><%= blogList[i] %></p>
            </div>
            
            <% } }%>
   <% } else{ %>
        <h1>Create your First Post!</h1> 
        <a href="/create">
            <div class="create-button">
                Blog +
            </div>
        </a>
   <% } %> 
    
</body>
</html>